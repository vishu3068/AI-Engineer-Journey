# Day 12 — Encapsulation and Polymorphism

## What is Encapsulation?
encapsulation is the imp techniques in oops which get methods and data inside class bounded and controls how data data is accessed and modified
## Public Attribute
public attribute can be accessed from anywhere
## Protected Convention (_name)
it is used internerlly to protect data we can access data only if necessery
## Private-like Attribute (__name)
it is secured and protected data we cannot directly access data
## What is a Getter?
getter is a method which returns the value of protected data
## What is a Setter?
setter is a method which is used to modify the protected data
## Why Validate Data Before Updating It?
it helps to tackle incorrect value or data
## What is Polymorphism?
polymorphism is the nothing but having same methods for diff classes and accessing every class's method with unique method operations
## Method Overriding as Polymorphism
printing different meaning for same method in different classes
## Polymorphism with Different Classes
each object of different class prints its function if method exits in that class
## What is Duck Typing?
python only checks the methods behaviour does not focus on class and give results